#include <stdio.h>

int main(void)
{
    double x, y, std;

    printf("Height: "); scanf("%lf", &x);
    printf("Body Weight: "); scanf("%lf", &y);
    printf("Standard Deviation is %.2lf\n", std = (x - 100) * 0.9);

    if (y - std > 8.0)
        printf("You are a little fat \n");

  return 0;
}
